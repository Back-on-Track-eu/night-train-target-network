services:
  openrailrouting-infra-2026:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openrailrouting-infra-2026
    ports:
      - "8989:8989"
      - "8990:8990"
    volumes:
      - ./data-infra-2026:/app/data
      - ./graph-cache-infra-2026:/app/graph-cache
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8989/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s