# Copy to .env on the server (chmod 600) — never commit this file.
# Generate strong values: openssl rand -base64 24

# PostgreSQL — demo instance (separate from the prod stack's DB)
POSTGRES_USER=tn_demo
POSTGRES_PASSWORD=
POSTGRES_DB=target_network_demo_db
