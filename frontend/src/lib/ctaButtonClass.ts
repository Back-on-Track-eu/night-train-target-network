// Shared styling for the "Suggest a new route" pill — the one call to action
// that repeats across the landing intro and the foot of the gallery list.
// Kept here so the two stay identical rather than drifting apart.
export const ctaButtonClass =
  'flex cursor-pointer items-center gap-1.5 rounded-full bg-primary-50/10 px-5 py-2 text-sm text-primary-50 transition hover:bg-primary-50/20'
